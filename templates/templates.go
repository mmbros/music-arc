// Generated by /tmp/go-build603709249/command-line-arguments/_obj/exe/gen-templates; DO NOT EDIT
// Creation date: 2016-01-24 09:39:20.528448448 +0100 CET

package templates

import (
	"html/template"
	"io"
	"path/filepath"
)

// PageEnum type definition
type PageEnum uint8

// PageEnum constants
const (
	PageAlbum PageEnum = iota
	PageAlbumList
	PageArtist
	PageArtistsList
	PageCoverFront
	PageCoverFront10
	PagePlaylist
	PagePlaylistsList
)

const mTemplatesFolder = "templates/music-arc"

var mTemplates [8]*template.Template

func init() {
	var templatesFiles = [...]string{"_base.tmpl", "_browser-sync.tmpl", "partials/artist.tmpl", "partials/album.tmpl", "album.tmpl", "album-list.tmpl", "partials/artists-list.tmpl", "artist.tmpl", "artists-list.tmpl", "cover/_base.tmpl", "cover/front.tmpl", "cover/front-10.tmpl", "partials/playlists-list.tmpl", "partials/playlist.tmpl", "playlist.tmpl", "playlists-list.tmpl"}
	var templatesIdxs = [...][]uint8{
		{0, 1, 2, 3, 4},    // album
		{0, 1, 5},          // album_list
		{0, 1, 6, 2, 7},    // artist
		{0, 1, 6, 8},       // artists_list
		{9, 1, 10},         // cover_front
		{9, 1, 11},         // cover_front_10
		{0, 1, 12, 13, 14}, // playlist
		{0, 1, 12, 15},     // playlists_list
	}

	// init base templates
	for i, idxs := range templatesIdxs {
		files := make([]string, len(idxs))
		for j, idx := range idxs {
			files[j] = templatesFiles[idx]
		}
		mTemplates[i] = template.Must(template.New(filepath.Base(files[0])).Funcs(funcMap).ParseFiles(files2paths(files)...))
	}

}

func files2paths(files []string) []string {
	var path string
	paths := make([]string, len(files))
	for i, file := range files {
		switch {
		case len(file) == 0, file[0] == '.', file[0] == filepath.Separator:
			path = file
		default:
			path = filepath.Join(mTemplatesFolder, file)
		}
		paths[i] = path
	}
	return paths
}

// Template returns the template.Template of the page
func (page PageEnum) Template() *template.Template {
	var idx = [...]uint8{0, 1, 2, 3, 4, 5, 6, 7}
	return mTemplates[idx[page]]
}

// Base returns the template name of the page
func (page PageEnum) Base() string {
	var bases = [...]string{""}
	var pi2bi = [...]uint8{0, 0, 0, 0, 0, 0, 0, 0}
	return bases[pi2bi[page]]
}

// Execute applies a parsed page template to the specified data object,
// writing the output to wr.
// If an error occurs executing the template or writing its output, execution
// stops, but partial results may already have been written to the output writer.
// A template may be executed safely in parallel.
func (page PageEnum) Execute(wr io.Writer, data interface{}) error {
	tmpl := page.Template()
	name := page.Base()
	if name == "" {
		return tmpl.Execute(wr, data)
	}
	return tmpl.ExecuteTemplate(wr, name, data)
}

/*
func main() {
  var page PageEnum
  page = PageAlbum

  wr := os.Stdout

  if err := page.Execute(wr, nil); err != nil {
	fmt.Print(err)
  }

}
*/
